#pragma once
#include <cstdio>
#include <cstring>
#include <string>
#include <vector>

namespace Logger {

void tabulate(const std::vector<std::string> &content, int nrow, int ncol);

} // namespace Logger